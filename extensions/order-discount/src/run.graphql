query RunInput {
  cart {
    buyerIdentity {
      customer {
        amountSpent {
          amount
        }
        numberOfOrders
      }
    }
  }
   discountNode {
    metafield(namespace: "order-discount", key: "function-configuration") {
      value
    }
  }
}
